import { setFirstName } from "../person/personSlice";

describe('personSlice', () => {
  describe('reducers', () => {
    test('Dummy test', () => {
      expect('Foo').toBe('Foo');
    });
  });
});
